#!/bin/sh
cd ${0%/*} || exit 1    # Run from this directory

# multiCompression
multiCompression/RiemannTube/./Allrun || cat log.multiCompression
multiCompression/tube/./Allrun        || cat log.potentialFoam
multiCompression/quadPiston/./Allrun  || cat log.potentialFoam
multiCompression/cylPiston/./Allrun   || cat log.potentialFoam

#------------------------------------------------------------------------------
