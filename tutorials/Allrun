#!/bin/sh
cd ${0%/*} || exit 1    # Run from this directory

for tutorialsDir in *Foam/; do
    for tutorialsBySolverDir in $tutorialsDir*/; do
        $tutorialsBySolverDir./Allrun || cat ${tutorialsBySolverDir}log.*
    done
done
